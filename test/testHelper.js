require('dotenv').config();

// before(done => {
//
// });
//
// beforeEach(done => {
//   // basecouriers.drop()
//   //   .then(() => basecouriers.createIndex({ 'location.coordinates': '2dsphere' }))
//   //   .then(() => done())
//   //   .catch(() => done())
// });